.row
  .small-10.small-centered.columns
    #content
      = render 'items/popular'
      %h3 Search results
    #results
      %table
        %tr
        -@items.each do |item|
          %td
            = image_tag(item.image)
            = item.name