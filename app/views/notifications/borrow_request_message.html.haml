%p= "Dear #{@owner.name},"
%p= "#{@borrower.name} has requested to borrow your #{@item.name}. Please follow the link to provide #{@borrower.name} with instructions on where and when to pick it up."
%p
  %a{:href => "http://somethingborrowed.herokuapp.com/items/borrow_instructions?borrower=#{@borrower.id}&item=#{@item.id}&owner=#{@owner.id}"} Borrow Instructions Form
%p Thank you,
= image_tag attachments['sblogo.jpg'].url
%p Something Borrowed Staff
