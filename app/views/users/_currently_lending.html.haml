%table
  %tr
    %th
    %th Name
    %th Description
    %th Instructions
    %th Due Date
    %th Borrower
    -@auth.currently_lending.each do |borrow|
      %tr
        %td= image_tag(borrow.item.image)
        %td= borrow.item.name.titleize
        %td= borrow.item.description
        %td= borrow.item.instructions
        %td= borrow.item.available_until
        %td= borrow.borrower.name

